<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Cuenta</title>
    <style>
        body {
            background-image: url('publico/shutterstock_1774527986.jpg');
            background-size: cover;
            background-position: center;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            color: #ffffff;
        }
        form {
            width: 80%;
            max-width: 400px;
            padding: 20px;
            background-color: #00acd8;
            justify-content: center;
            align-items: center;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(147, 98, 98, 0.1);
        }
        input[type="text"], input[type="email"], input[type="tel"], input[type="password"] {
            width: calc(95% - 20px);
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #000000;
            border-radius: 4px;
        }
        input[type="submit"] {
            width: calc(100% - 20px);
            padding: 10px;
            background-color: #ffffff;
            border: none;
            border-radius: 4px;
            color: #00acd8;
            font-size: 14px;
            cursor: pointer;
        }
        input[type="submit"]:hover {
            background-color: #ffffff;
        }
        .message {
            color: green;
            font-weight: bold;
            margin-top: 10px;
            text-align: center;
            display: none;
        }
        .error {
            color: red;
            font-weight: bold;
            margin-top: 10px;
            text-align: center;
            display: none;
        }
        .logo {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 20px;
        }
        .logo img {
            width: 80px;
            height: 30px;
        }
    </style>

</head>
<body>
    <form id="registroForm" action="/registro" method="POST">
        <div class="logo">
            <img src="/publico/sosya-blanco-200px.png" alt="Logo">
        </div>
        <div class="container">
            <h1>Registro de Cuenta</h1>       
            <label for="nombre">Nombre de Usuario:</label>
            <input type="text" id="nombre" name="nombre" value="<%= datosRegistro ? datosRegistro.nombre : '' %>" required>
            <label for="email">Correo Electrónico:</label>
            <input type="text" id="correo" name="correo" value="<%= datosRegistro ? datosRegistro.correo : '' %>" required>
            <label for="password">Contraseña:</label>
            <input type="password" id="password" name="password" required>
            <input type="submit" value="Registrarse">
        </div>
    </form>
</body>
</html>
